# PulseGuard Test Suite

Simple test suite for the password manager:

- **Unit (test_models.py)** — Tests for data models and vault operations

## Running locally

```bash
uv pip install -e .[dev,test]
uv run pytest
```
